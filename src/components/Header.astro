---
const { name, surname, subtitle, avatarUrl } = Astro.props;
---

<header class="flex flex-col sticky top-0 w-full pt-8 z-10 backdrop-blur-md">
  <div class="flex items-center justify-center gap-4">
    <img class="w-16 h-16 rounded-full" src={avatarUrl} alt="avatar" />
    <div class="font-medium dark:text-white">
      <div class="font-bold text-2xl tracking-wide">
        <span class="font-extralight">{name}</span>
        <span class="text-yellow-500 font-extrabold">{surname}</span>
      </div>
      <div class="text-sm text-gray-500 dark:text-gray-400">
        {subtitle}
      </div>
    </div>
  </div>
  <p
    class="w-full py-4 text-pretty tracking-wide font-semibold text-center text-yellow-300 opacity-60"
  >
    Software Developer, Tech lover & Gymrat in process
  </p>
  <h6 class="font-thin px-8">Bookmarks:</h6>
</header>
