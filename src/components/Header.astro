---
const { name, surname, subtitle, avatarUrl } = Astro.props;
---

<header
  class="flex
    flex-col
    sticky
    top-0
    w-full
    pt-8
    z-10
    backdrop-blur-md
    bg-gradient-to-b
    from-yellow-500/20
    to-transparent"
>
  <div class="flex items-center justify-center gap-4">
    <img
      class="w-16 h-16 rounded-full shadow-2xl"
      src={avatarUrl}
      alt="avatar"
    />
    <div class="font-medium dark:text-white">
      <div class="font-bold text-2xl tracking-wide">
        <span class="font-extralight">{name}</span>
        <span class="text-yellow-500 font-extrabold">{surname}</span>
      </div>
      <!-- <div class="text-sm text-gray-500 dark:text-gray-400">
        {subtitle}
      </div> -->
    </div>
  </div>
  <p
    class="w-full py-4 px-8 text-pretty tracking-wide font-light text-center text-gray-300 opacity-80"
  >
    Software Developer, Tech lover & Gymrat in progress
  </p>
  <h6 class="font-light px-8 text-yellow-500 text-center">Bookmarks</h6>
</header>
